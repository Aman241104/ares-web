"use client";

export default function WebPartnerClient() {
  return (
    <div className="pt-24 bg-[#050B0A] min-h-[30vh] overflow-hidden relative flex flex-col justify-center items-center">
      {/* Intentionally left empty as the Global CTA at the bottom of the page now serves the purpose of this entire page */}
    </div>
  );
}
